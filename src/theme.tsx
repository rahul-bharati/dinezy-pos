'use client'
import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#FFFAFF',
    },
    text: {
      primary: '#FFFAFF',
    }
  },
})

export default theme
